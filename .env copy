APP_NAME="G-MANAGER"
APP_ENV=local
APP_KEY=base64:XUzJRKVdIcvqM7Bywr/4OZelRVpw0VRFZGzHanEVOSw= 
APP_DEBUG=true
APP_URL=http://localhost # Sesuaikan dengan server_name di Nginx

APP_LOCALE=id # Diubah ke Bahasa Indonesia
APP_FALLBACK_LOCALE=id
APP_FAKER_LOCALE=id_ID

APP_MAINTENANCE_DRIVER=file

PHP_CLI_SERVER_WORKERS=4
BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# --- KONEKSI DATABASE POSTGRESQL ---
DB_CONNECTION=pgsql # DIUBAH dari sqlite ke pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=gman_db # Pastikan nama database ini ada di PostgreSQL
DB_USERNAME=postgres # Pastikan user ini ada dan punya akses ke DB
DB_PASSWORD=MyStr0ngP@ssw0rd! # Pastikan password ini benar

# --- KONFIGURASI REDIS UNTUK BROADCAST, CACHE, QUEUE, & SESSION ---
BROADCAST_CONNECTION=redis # Diubah dari log untuk konsistensi
FILESYSTEM_DISK=local
QUEUE_CONNECTION=redis # Sudah benar
CACHE_STORE=redis # DIUBAH dari database ke redis
SESSION_DRIVER=redis # Sudah benar

REDIS_CLIENT=predis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# --- KONFIGURASI EMAIL (log cocok untuk development) ---
MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=1025 # Port mail-catcher/mailhog yang umum, bukan 2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="no-reply@gdrive.test" # Ganti dengan domain Anda
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"